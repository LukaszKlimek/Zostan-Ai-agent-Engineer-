# 🔢 Logarytmy i Funkcje Wykładnicze

---

## 1. Funkcja wykładnicza — wzrost który przyspiesza

Zacznijmy od pytania: co się dzieje z liczbą bakterii w szalkach Petriego? Jedna bakteria dzieli się na dwie, każda z dwóch dzieli się na kolejne dwie, i tak dalej. Po każdej godzinie liczba bakterii się podwaja.

```
Godzina 0:  1 bakteria
Godzina 1:  2 bakterie
Godzina 2:  4 bakterie
Godzina 3:  8 bakterii
Godzina 4:  16 bakterii
Godzina 10: 1 024 bakterie
Godzina 20: 1 048 576 bakterii  ← milion!
Godzina 40: ponad bilion!
```

To jest wzrost wykładniczy. Liczba bakterii po `n` godzinach to `2ⁿ`. Wykładnik (ta górna liczba) to właśnie czas, stąd nazwa "funkcja wykładnicza".

**Ogólna postać: `f(x) = aˣ`**, gdzie `a` to podstawa (w przykładzie z bakteriami a = 2).

Kluczowa cecha wzrostu wykładniczego: rośnie coraz szybciej. Różnica między godziną 1 a 2 to 2 bakterie. Między godziną 19 a 20 to już 524 288 bakterii — a te godziny są tak samo od siebie odległe!

```
Wykres f(x) = 2ˣ:

y
8│              *  (3, 8)
 │
4│          *  (2, 4)
 │
2│       * (1, 2)
 │
1│────*  (0, 1)
 │ *  (-1, 0.5)
 └─────────────────── x
  -1   0   1   2   3
```

Ważne: dla x ujemnych wartości funkcja jest ułamkowa (bliska zeru, ale nigdy nie osiąga zera). Krzywa nigdy nie dotyka osi poziomej.

> **W AI:** Wzrost liczby parametrów w dużych modelach językowych (jak GPT, Claude) to właśnie wzrost wykładniczy. Modele z 2020 roku miały miliardy parametrów, z 2023 — setki miliardów. Rozumienie wykładniczego wzrostu pomaga rozumieć dlaczego AI tak szybko się rozwija i dlaczego zmiany są tak dramatyczne.

---

## 2. Liczba e — skąd się wzięła?

W matematyce i fizyce najczęściej spotkasz nie `2ˣ` ani `10ˣ`, ale `eˣ`, gdzie `e ≈ 2.718`. Dlaczego akurat ta dziwna liczba?

Wyobraź sobie bank. Loknujesz 1 złoty przy 100% oprocentowaniu rocznym.

```
Odsetki raz na rok:    1 × (1 + 1)¹ = 2.00 zł
Odsetki co pół roku:   1 × (1 + 1/2)² = 2.25 zł
Odsetki co miesiąc:    1 × (1 + 1/12)¹² ≈ 2.61 zł
Odsetki co dzień:      1 × (1 + 1/365)³⁶⁵ ≈ 2.714 zł
Odsetki co sekundę:    ≈ 2.71827...
Odsetki "ciągle":      → 2.71828... = e
```

Gdy oprocentowanie jest naliczane nieskończenie często (czyli "ciągle"), wynik dąży do liczby `e`. To jest właśnie `e` — granica pewnego procesu ciągłego wzrostu.

Dlatego `eˣ` pojawia się wszędzie tam, gdzie mamy do czynienia z ciągłymi procesami wzrostu lub zaniku: rozpad promieniotwórczy, chłodzenie przedmiotów, wzrost populacji, naładowanie kondensatora...

Specjalna właściwość `eˣ`: pochodna z `eˣ` to `eˣ`. To jedyna funkcja, która jest swoją własną pochodną. Ta wyjątkowa cecha sprawia, że obliczenia z `e` są szczególnie eleganckie i wygodne w matematyce.

> **W AI:** `eˣ` pojawia się w funkcji sigmoid i softmax — kluczowych funkcjach używanych w modelach klasyfikacyjnych. Sigmoid zamienia dowolną liczbę na wartość między 0 a 1 (czyli na "prawdopodobieństwo"). Nie musisz teraz wiedzieć jak dokładnie działają, ale gdy je zobaczysz, zapamiętaj: pod spodem jest właśnie `eˣ`.

---

## 3. Logarytm — pytanie odwrotne do potęgowania

Potęgowanie odpowiada na pytanie: **"2 do potęgi 10 to ile?"** (odpowiedź: 1024).

Logarytm odpowiada na pytanie odwrotne: **"2 do jakiej potęgi daje 1024?"** (odpowiedź: 10).

```
Potęgowanie:   2¹⁰ = 1024
Logarytm:      log₂(1024) = 10

To jest to samo stwierdzenie, napisane w dwóch różnych "kierunkach".
```

Ogólna definicja: `logₐ(x) = y` oznacza dokładnie to samo co `aʸ = x`.

Kilka przykładów żeby poczuć co to znaczy:

```
log₂(8) = 3      bo 2³ = 8       ("2 do jakiej potęgi daje 8? Do trzeciej.")
log₂(1) = 0      bo 2⁰ = 1       ("2 do jakiej potęgi daje 1? Do zerowej.")
log₂(1/2) = -1   bo 2⁻¹ = 0.5   ("2 do jakiej potęgi daje 1/2? Do minus jedynki.")

log₁₀(1000) = 3  bo 10³ = 1000
log₁₀(100) = 2   bo 10² = 100
log₁₀(10) = 1    bo 10¹ = 10
log₁₀(1) = 0     bo 10⁰ = 1
```

Intuicja log₁₀: logarytm przy podstawie 10 mówi mniej więcej "ile cyfr ma ta liczba minus jeden". Tysiąc ma 4 cyfry, log₁₀(1000) = 3. Milion ma 7 cyfr, log₁₀(1000000) = 6.

> **W AI:** Skale logarytmiczne są wygodne gdy dane mają bardzo szeroki zakres. Populacje miast wahają się od kilkuset do kilkudziesięciu milionów — na skali logarytmicznej te wartości stają się porównywalne. Wiele hiperparametrów w AI (jak "tempo uczenia") dobrze dobierać właśnie w skali logarytmicznej: 0.0001, 0.001, 0.01, 0.1 to równe odstępy na skali log.

---

## 4. Rodzaje logarytmów

W praktyce spotkasz trzy główne "smaki" logarytmu:

**Logarytm przy podstawie 10 (`log₁₀`)** — często zapisywany po prostu jako `log`. Historycznie ważny przy obliczeniach inżynierskich. Pojawia się w skalach takich jak decybele (głośność dźwięku) czy skala Richtera (magnitud trzęsień ziemi).

**Logarytm naturalny (`ln`)** — logarytm przy podstawie e ≈ 2.718. To najczęściej używany logarytm w matematyce i fizyce. Zapis `ln(x)` to skrót od łacińskiego *logarithmus naturalis*.

```
ln(1) = 0     bo e⁰ = 1
ln(e) = 1     bo e¹ = e
ln(e²) = 2    bo e² = e²
ln(7.39) ≈ 2  bo e² ≈ 7.39
```

**Logarytm przy podstawie 2 (`log₂`)** — ważny w informatyce. Odpowiada na pytanie "ile razy muszę podzielić przez 2?". Użyteczny przy analizie algorytmów.

```
log₂(1024) = 10   → żeby dojść od 1024 do 1, trzeba 10 razy podzielić przez 2
log₂(256)  = 8
log₂(32)   = 5
```

Wszystkie trzy są "tą samą ideą" przy różnych podstawach — pytają "do jakiej potęgi?".

> **W AI:** W kontekście uczenia maszynowego najczęściej zobaczysz `ln` (logarytm naturalny) używany w funkcjach straty — miarach jak bardzo model się myli. Szczegóły tych wzorów poznasz w kolejnych krokach.

---

## 5. Prawa logarytmów — trzy reguły których warto się nauczyć

Prawa logarytmów wynikają z praw potęgowania. Są użyteczne przy upraszczaniu wyrażeń.

**Prawo 1: Logarytm iloczynu = suma logarytmów**

```
log(a × b) = log(a) + log(b)

Przykłady:
  log₂(4 × 8) = log₂(4) + log₂(8) = 2 + 3 = 5
  Sprawdzenie: 4 × 8 = 32 = 2⁵  ✅

  ln(e × e²) = ln(e) + ln(e²) = 1 + 2 = 3
  Sprawdzenie: e × e² = e³, ln(e³) = 3  ✅
```

Intuicja: mnożenie liczb to dodawanie wykładników, a logarytm to "odczytywanie wykładnika".

**Prawo 2: Logarytm ilorazu = różnica logarytmów**

```
log(a / b) = log(a) - log(b)

Przykłady:
  log₂(32 / 4) = log₂(32) - log₂(4) = 5 - 2 = 3
  Sprawdzenie: 32/4 = 8 = 2³  ✅
```

**Prawo 3: Logarytm potęgi = wykładnik jako mnożnik**

```
log(aⁿ) = n × log(a)

Przykłady:
  log₂(2⁷) = 7 × log₂(2) = 7 × 1 = 7       ✅
  log(10⁵) = 5 × log(10) = 5 × 1 = 5        ✅
  ln(e^(-3)) = -3 × ln(e) = -3 × 1 = -3     ✅
```

To prawo jest szczególnie użyteczne: pozwala "wyciągnąć" wykładnik przed logarytm.

> **W AI:** Prawa logarytmów są używane do uproszczenia wzorów w teorii za modelami. Na przykład gdy mamy iloczyn wielu prawdopodobieństw (co szybko daje bardzo małe liczby), logarytm zamienia go w sumę — co jest numerycznie wygodniejsze dla komputerów. Ale to szczegół, z którym spotkasz się głębiej w kursie.

---

## 6. Wykres logarytmu — odwrotność wykresu wykładniczej

Wykresy `eˣ` i `ln(x)` są lustrzanym odbiciem siebie nawzajem względem prostej `y = x`. To dlatego, że są funkcjami odwrotnymi.

```
y = eˣ:
  x = -2:  e⁻² ≈ 0.14
  x = 0:   e⁰ = 1
  x = 1:   e¹ ≈ 2.72
  x = 2:   e² ≈ 7.39

y = ln(x):  (dziedzina tylko dla x > 0!)
  x = 0.14: ln(0.14) ≈ -2
  x = 1:    ln(1) = 0
  x = 2.72: ln(2.72) ≈ 1
  x = 7.39: ln(7.39) ≈ 2
```

Kluczowe cechy wykresu logarytmu:
- Rośnie dla wszystkich x > 0, ale **coraz wolniej** — odwrotnie niż wykładnicza, która rośnie coraz szybciej
- W punkcie x = 1 zawsze przechodzi przez zero (bo `logₐ(1) = 0` dla każdej podstawy)
- Nie istnieje dla x ≤ 0 — logarytm z zera lub liczby ujemnej jest niezdefiniowany

```
y = ln(x)

y
│
2│                    *  (7.39, 2)
│
1│          * (2.72, 1)
│
0│────* (1, 0)────────────────── x
│ * (0.5, -0.7)  1   2   3   4   5
-1│
│
```

> **W AI:** Logarytm "spłaszcza" szerokie skale. Jeśli dane mają rozkład silnie skośny (np. dochody ludzi — większość zarabia mało, ale kilka osób zarabia astronomicznie), logarytm z tych danych daje ładniejszy, bardziej symetryczny rozkład. Transformacja logarytmiczna danych to jeden z typowych sposobów na poprawę jakości danych przed trenowaniem modelu.

---

## 7. Jak logarytm mierzy błąd modelu?

To jest miejsce gdzie logarytmy naprawdę mają znaczenie w AI, i warto to zrozumieć intuicyjnie.

Wyobraź sobie model, który ma odpowiedzieć: "czy na zdjęciu jest kot?" Daje odpowiedź jako prawdopodobieństwo: 0.9 znaczy "90% pewności że tak".

Jak "ukarać" model za złą odpowiedź? Chcemy:
- Małą karę gdy model powiedział 0.9 i miał rację (był pewny i dobrze)
- Dużą karę gdy model powiedział 0.1 i się mylił (był pewny i źle)
- Szczególnie dużą karę gdy model był bardzo pewny błędnej odpowiedzi

Logarytm ma dokładnie takie właściwości:

```
Prawdziwa odpowiedź to "tak" (1). Model powiedział prawdopodobieństwo p.
Kara = -ln(p)

Gdy p = 0.99:  -ln(0.99) ≈ 0.01   ← prawie zero, mała kara
Gdy p = 0.90:  -ln(0.90) ≈ 0.11
Gdy p = 0.50:  -ln(0.50) ≈ 0.69
Gdy p = 0.10:  -ln(0.10) ≈ 2.30   ← duża kara
Gdy p = 0.01:  -ln(0.01) ≈ 4.61   ← bardzo duża kara!
```

Gdy model jest bardzo pewny siebie i się myli (p = 0.01, a prawda to 1), kara jest ogromna. Gdy jest skromny lub w miarę pewny i ma rację — kara mała. To zachowanie wynika bezpośrednio z kształtu wykresu logarytmu.

Ta funkcja kary (zwana cross-entropy loss) to jeden z najczęściej używanych sposobów mierzenia błędu w modelach klasyfikacyjnych.

> **W AI:** Cross-entropy loss to standardowa funkcja straty dla zadań klasyfikacji — "czy to spam?", "co jest na zdjęciu?", "jakiego gatunku jest ten tekst?". Teraz wiesz skąd pochodzi i dlaczego logarytm jest do tego używany: kara logarytmiczna silnie penalizuje pewność siebie połączoną z błędem.

---

## ✅ Sprawdź się

1. Ile wynosi `log₂(64)`? (Podpowiedź: 2 do jakiej potęgi = 64?)
2. Uprość: `ln(x⁵) - ln(x²)`.
3. Dlaczego logarytm jest przydatny gdy dane mają bardzo szeroki zakres wartości (np. od 1 do 1 000 000)?
4. Wyjaśnij własnymi słowami: dlaczego `-ln(0.01)` jest większe niż `-ln(0.9)`?

<details>
<summary>Odpowiedzi</summary>

1. `log₂(64) = 6`, bo `2⁶ = 64`

2. `ln(x⁵) - ln(x²) = 5·ln(x) - 2·ln(x) = 3·ln(x)` (lub `ln(x⁵/x²) = ln(x³) = 3ln(x)`)

3. Logarytm "ściska" duże wartości — 1 000 000 po logarytmie przy podstawie 10 to tylko 6. Dane z szerokim zakresem stają się porównywalne i mają ładniejszy rozkład.

4. `ln(0.01) ≈ -4.6`, więc `-ln(0.01) ≈ 4.6`. `ln(0.9) ≈ -0.1`, więc `-ln(0.9) ≈ 0.1`. Logarytm bardzo małej liczby jest dużą liczbą ujemną — bo żeby dostać bardzo małą liczbę z potęgowania, potrzeba dużego ujemnego wykładnika. Gdy "odwracamy" to przez minus, dostajemy dużą karę.

</details>
