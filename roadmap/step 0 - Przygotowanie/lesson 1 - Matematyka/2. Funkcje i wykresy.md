# 📈 Funkcje i Wykresy

---

## 1. Czym jest funkcja?

Wyobraź sobie automat z kawą. Wciskasz przycisk "espresso" i dostajesz espresso. Wciskasz "latte" i dostajesz latte. Kluczowe jest to, że jeden przycisk daje zawsze **dokładnie jeden** określony napój — nigdy nie zdarza się, że wciskasz espresso i raz wychodzi kawa, a raz zupa.

Właśnie tak działa funkcja w matematyce. **Funkcja to reguła, która każdej wartości wejściowej przypisuje dokładnie jedną wartość wyjściową.** Podajesz liczbę, dostajesz liczbę.

Zapisujemy to tak: `y = f(x)`, co czytamy "y jest funkcją x". Litera `f` to po prostu nazwa tej reguły. `x` to wejście, `y` to wyjście.

```
Przykład: f(x) = 2x + 1

f(3) = 2 × 3 + 1 = 7       → dla wejścia 3, wyjście to 7
f(5) = 2 × 5 + 1 = 11      → dla wejścia 5, wyjście to 11
f(0) = 2 × 0 + 1 = 1       → dla wejścia 0, wyjście to 1
```

Można sobie wyobrazić funkcję jak maszynkę: coś wrzucasz, ona przetwarza, coś wyrzuca.

```
        ┌─────────────────┐
 x ────►│  f(x) = 2x + 1  ├────► y
        └─────────────────┘
```

> **W AI:** Każdy program AI, który coś przewiduje (np. cenę mieszkania, czy email to spam), jest w gruncie rzeczy funkcją. Dostajesz dane wejściowe (np. powierzchnia: 60 m²), dostajesz wynik (np. cena: 450 000 zł). Cały model AI to po prostu bardzo skomplikowana funkcja matematyczna.

---

## 2. Dziedzina i zbiór wartości

Niektóre funkcje mają ograniczenia — nie możesz podać im dowolnego wejścia.

**Dziedzina** to zbiór wszystkich wartości, które możesz bezpiecznie podać na wejście.

Pomyśl o funkcji `f(x) = √x` (pierwiastek kwadratowy z x). Nie możesz wziąć pierwiastka z liczby ujemnej — to po prostu nie daje wyniku w liczbach rzeczywistych. Dlatego dziedzina tej funkcji to "wszystkie liczby nieujemne", czyli x ≥ 0.

```
f(x) = √x
  f(4)  = 2   ✅   4 jest w dziedzinie
  f(0)  = 0   ✅   0 jest w dziedzinie
  f(-4) = ?   ❌   -4 nie jest w dziedzinie!
```

Inny przykład: funkcja `f(x) = 1/x`. Nie możesz dzielić przez zero — to niedozwolone w matematyce. Więc dziedzina to wszystkie liczby oprócz zera.

**Zbiór wartości** to z kolei wszystkie możliwe wyniki, które funkcja może wyprodukować. Dla `f(x) = x²` wynikiem zawsze jest liczba nieujemna — nie da się uzyskać wyniku ujemnego, bo kwadrat jakiejkolwiek liczby jest zawsze ≥ 0.

> **W AI:** Zbiór wartości funkcji ma znaczenie praktyczne. Jeśli chcesz żeby program zwracał "prawdopodobieństwo" (np. 73% szansy na deszcz), to wynik musi być między 0 a 1. Do tego używa się specjalnych funkcji, które zawsze dają wynik w tym przedziale — ale o tym będziesz się uczyć w dalszych krokach kursu.

---

## 3. Funkcja liniowa — najprostsza ze wszystkich

Funkcja liniowa to `f(x) = ax + b`. Jej wykres to prosta linia — stąd nazwa.

Dwa parametry:
- `a` — nachylenie, czyli "jak stroma jest linia". Gdy a = 2, to za każdym razem gdy x rośnie o 1, y rośnie o 2.
- `b` — gdzie linia przecina oś pionową, gdy x = 0.

```
Przykład: f(x) = 2x + 1

x:   0    1    2    3    4
y:   1    3    5    7    9
        +2   +2   +2   +2    ← y rośnie o 2 za każdym razem
```

Wykres wygląda tak:

```
y
│              * (4, 9)
│           *
│        * (2, 5)
│     * (1, 3)
│  * (0, 1) ← tu przecina oś Y, bo b = 1
└──────────────── x
0  1  2  3  4
```

Gdy `a > 0` — linia idzie w górę. Gdy `a < 0` — linia idzie w dół. Gdy `a = 0` — linia pozioma.

> **W AI:** Funkcja liniowa to fundament najprostszego rodzaju modelu przewidującego — regresji liniowej. Wyobraź sobie, że chcesz przewidzieć cenę mieszkania na podstawie jego powierzchni. Linia prosta to najprostszy opis tego związku: "za każdy dodatkowy metr cena rośnie o X złotych". Bardziej skomplikowane modele buduje się z wielu takich prostych funkcji.

---

## 4. Funkcja kwadratowa — pojawia się parabola

Funkcja kwadratowa to `f(x) = ax² + bx + c`. Jej wykres to krzywa w kształcie litery U (lub odwróconego U) — zwana parabolą.

Najbardziej intuicyjny przykład to `f(x) = x²`:

```
x:   -3   -2   -1    0    1    2    3
y:    9    4    1    0    1    4    9
```

Zwróć uwagę, że zarówno x = 3 jak i x = -3 daje ten sam wynik: 9. Funkcja jest symetryczna.

```
y
9│*             *
 │
4│  *         *
 │
1│    *     *
 │
0│      *         ← minimum, "dno" paraboli
 └──────────────── x
  -3 -2 -1  0  1  2  3
```

Gdy `a > 0` — parabola otwarta w górę (jak miska), ma **minimum**.
Gdy `a < 0` — parabola otwarta w dół (jak kopiec), ma **maksimum**.

> **W AI:** Kiedy program AI się uczy, stara się zminimalizować swój błąd. Błąd jako funkcja parametrów modelu często ma kształt przypominający parabolę — z jednym wyraźnym dnem. Cały proces uczenia się polega na "znajdowaniu dna tej paraboli". W rzeczywistości jest to wielowymiarowe i bardziej skomplikowane, ale ta intuicja U-kształtowanej funkcji jest dobrym punktem startowym.

---

## 5. Wartość bezwzględna — zawsze nieujemna

Wartość bezwzględna z liczby `x`, pisana `|x|`, to po prostu "odległość tej liczby od zera na osi liczbowej" — zawsze nieujemna.

```
|5|  = 5     → 5 jest 5 kroków od zera
|-5| = 5     → -5 też jest 5 kroków od zera
|0|  = 0
|-3.7| = 3.7
```

Formalny zapis:
```
|x| = x    gdy x ≥ 0
|x| = -x   gdy x < 0    (czyli "odwracamy znak")
```

Wykres ma kształt litery V — symetryczny:

```
y
│\             /
│ \           /
│  \         /
│   \       /
│    \     /
│     \   /
│      \ /
│       *  ← wierzchołek w (0, 0)
└──────────────── x
```

> **W AI:** Wartość bezwzględna pojawia się przy mierzeniu błędów. Jeśli model przewidział cenę 450 000 zł, a prawdziwa cena to 480 000 zł, błąd wynosi |450 000 - 480 000| = 30 000 zł. Bez wartości bezwzględnej wyniki "za wysokie" i "za niskie" mogłyby się znosić, co dawałoby fałszywe wrażenie małego błędu.

---

## 6. Złożenie funkcji — funkcja w funkcji

Złożenie to sytuacja, gdy wynik jednej funkcji staje się wejściem dla drugiej.

Wyobraź sobie dwie maszynki ustawione jedna za drugą: wrzucasz coś do pierwszej, dostajesz wynik, wrzucasz go do drugiej.

```
        ┌───┐     ┌───┐
 x ────►│ g ├────►│ f ├────► f(g(x))
        └───┘     └───┘
```

Przykład krok po kroku:

```
g(x) = x + 3     (dodaj 3 do wejścia)
f(x) = x²        (podnieś wejście do kwadratu)

Oblicz f(g(2)):
  Krok 1: najpierw g    →  g(2) = 2 + 3 = 5
  Krok 2: wynik do f    →  f(5) = 5² = 25

  Odpowiedź: f(g(2)) = 25
```

Możesz też napisać ogólny wzór:
```
f(g(x)) = f(x + 3) = (x + 3)²

Sprawdzenie: (2 + 3)² = 5² = 25  ✅
```

**Ważne: kolejność ma znaczenie!** `f(g(x))` to nie to samo co `g(f(x))`.

```
g(f(2)) = g(2²) = g(4) = 4 + 3 = 7   ← zupełnie inny wynik!
```

> **W AI:** Zaawansowane modele AI (np. sieci neuronowe) to dosłownie wielokrotne złożenia funkcji — jedna po drugiej, niekiedy setki warstw. Dane wchodzą, przechodzą przez pierwszą funkcję, wynik idzie do drugiej, potem do trzeciej... i tak dalej, aż do finalnego wyniku. Rozumienie złożenia funkcji to klucz do rozumienia jak te modele działają.

---

## 7. Jak czytać wykres?

Wykres funkcji to graficzny obraz jej zachowania. Kilka umiejętności, które warto mieć:

**Sprawdzenie czy funkcja rośnie czy maleje:** Patrząc od lewej do prawej — jeśli wykres idzie w górę, funkcja rośnie. Jeśli w dół — maleje.

**Znalezienie miejsc zerowych:** To punkty, gdzie wykres przecina poziomą oś (oś x). W tych punktach f(x) = 0. Na przykład funkcja `f(x) = x² - 4` przecina oś x w punktach x = 2 i x = -2.

**Minimum i maksimum:** To "dna" i "szczyty" wykresu. Parabola otwarta w górę ma jedno minimum. W punkcie minimum wykres "zawraca" — przestaje opadać i zaczyna rosnąć.

```
Przykładowy wykres z różnymi cechami:

y
│        *  ← maksimum lokalne
│      *   *
│    *       *
│───*─────────*──────  ← miejsca zerowe
│  *           *
│*               *
└──────────────────────── x
↑                  ↑
lewa krańcowa    prawa krańcowa
wartość         wartość
```

**Asymptoty:** Niektóre wykresy zbliżają się do prostej, ale nigdy jej nie dotykają. Na przykład `f(x) = 1/x` zbliża się do osi x dla dużych x, ale jej nie osiąga.

> **W AI:** Umiejętność czytania wykresów przydaje się przy analizie jak model się uczy — czy jego błąd maleje? Czy osiągnął minimum? Czy nie "skacze" w górę i w dół? Wykres błędu w czasie trenowania to jeden z podstawowych narzędzi oceny modelu.

---

## ✅ Sprawdź się

1. Oblicz `f(4)` dla `f(x) = 3x - 2`.
2. Jaka jest dziedzina funkcji `f(x) = 1/(x - 5)`?
3. Dana jest `g(x) = x + 2` i `f(x) = x²`. Ile wynosi `f(g(3))`?
4. Narysuj szkic wykresu `f(x) = -x²` — czy ma minimum czy maksimum?

<details>
<summary>Odpowiedzi</summary>

1. `f(4) = 3 × 4 - 2 = 10`
2. Wszystkie liczby oprócz x = 5 (tam dzielnik byłby zerem)
3. `g(3) = 5`, potem `f(5) = 25`
4. Ma maksimum w punkcie (0, 0) — parabola otwarta w dół (a = -1 < 0)

</details>
